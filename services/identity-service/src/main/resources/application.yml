server:
  port: 8083
spring:
  application:
    name: identity-service
  datasource:
    url: jdbc:postgresql://localhost:5432/smartbank
    username: smartbank
    password: smartbank123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false

jwt:
  secret: SmartBankSuperSecretKeyForJWTTokenSigning2026MustBeAtLeast32BytesLong!
  expiration-ms: 86400000    # 24 hours

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    com.smartbank.identity: DEBUG